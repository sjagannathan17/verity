# Copy to .env.local to enable the LIVE agent. With no key, Verity runs in MOCK
# mode: a deterministic, identically-shaped reasoning trace so the demo always works.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
